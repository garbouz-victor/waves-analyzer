[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sloshing-visualization"
version = "0.1.0"
description = "Linear viscous free-surface Navier–Stokes with pinned contact points"
requires-python = ">=3.9"
dependencies = [
  "numpy>=1.21.5,<3",
  "scipy>=1.12,<2",
  "scikit-fem==11.0.0",
  "h5py>=3.6,<4",
  "PyYAML>=6,<7",
]

[project.optional-dependencies]
test = ["pytest>=7,<9", "sympy>=1.10,<2", "matplotlib>=3.5,<4", "pillow>=9,<13", "plotly>=5,<7"]
render = ["matplotlib>=3.5,<4", "pillow>=9,<13", "plotly>=5,<7"]

[project.scripts]
sloshing-run = "sloshing.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"sloshing.visualization" = ["*.html"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra -m 'not validation'"
markers = ["validation: slower spatial and temporal convergence measurements"]
